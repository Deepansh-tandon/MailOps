# Backend

# Optional fallback if not using OpenRouter
OPENAI_API_KEY=
LLM_MODEL=openai/gpt-4o-mini

# Legacy Google OAuth (optional; Composio now owns app auth)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/auth
GEMINI_API_KEY=

# Frontend
